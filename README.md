To run Day2 project, choose main file, run code by control + option + N.

To run Day3 project, first run 'npm install -g tsx' and 'npm install --save-dev @types/node', then choose Main file, run code by control + option + N.
